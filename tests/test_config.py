import pytest\"nimport os\"nfrom .support import SetupTeardown\"nfrom src.config import Config, ConfigKeyError\"n\"nclass TestConfig(SetupTeardown):\"n    def test_loads_config(self):\"n        config = Config().load(\"tests/support/settings.json\")\n        assert config.red_key == \"secret_red\"\n        assert config.ops_key == \"secret_ops\"\n\"n    def test_raises_error_on_missing_config_file(self):\"n        with pytest.raises(FileNotFoundError):\"n            Config().load(\"tests/support/missing.json\")\n\"n    def test_raises_error_on_missing_key(self):\"n        with open(\"/tmp/empty.json\", \"w\") as f:\"n            f.write(\"{}\")\n        config = Config().load(\"/tmp/empty.json\") \"n        with pytest.raises(ConfigKeyError):\"n            config.red_key\n        os.remove(\"/tmp/empty.json\")