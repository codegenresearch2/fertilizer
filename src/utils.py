from src.utils import url_join\\\ndef flatten(arg):\\"""Flatten a nested list."""\\\n    if not isinstance(arg, list):\\\n        return [arg]\\\n    return [x for sub in arg for x in flatten(sub)]\\\n\\\\\ndef url_join(*args):\\"""Join a list of paths into a single URL."""\\\n    args = [str(arg).lstrip('/') for arg in args if arg]\\\n    return '/'.join(args)\\