from src.utils import url_join\\ndef flatten(arg):\\\"""Flatten a nested list."""\\\\\\n  if not isinstance(arg, list):\\\\\\n    return [arg]\\\\\n  return [x for sub in arg for x in flatten(sub)]\\\\\n\\nfrom src.utils import url_join\\\\\ndef url_join(base, *paths):\\\"""Join a base URL with a list of paths."""\\\\\n  # Normalize the paths to ensure they start with a slash\\\\\n  paths = [path.lstrip('/') for path in paths]\\\\\n  # Join the paths\\\\\n  path = '/'.join([base] + paths)\\\\\n  # Remove any trailing slash\\\\\n  return path.rstrip('/')\\\\\n