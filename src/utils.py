from src.utils import url_join\n\ndef flatten(arg):\n    \"\"\"Flatten a nested list.\"\"\"\n    if not isinstance(arg, list):\n        return [arg]\n    return [x for sub in arg for x in flatten(sub)]\n\n\ndef url_join(*paths):\n    \"\"\"Join a list of paths into a single URL.\"\"\"\n    paths = [path.lstrip('/') for path in paths]\n    return '/'.join(paths)